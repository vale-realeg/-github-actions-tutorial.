name: Mi Primer Workflow

on:
  push:
    branches:
         - qa

jobs: 
   ejemplo:
      runs-on: ubuntu-latest
      env:
        PLATAFORMA: "AWS"
        REGION: "us-east-1"
      steps:
        - name: Mostrar plataforma solo si es AWS
          if: env.PLATAFORMA == 'AWS'
          run: | 
           echo "La Plataforma es AWS"
           
        
